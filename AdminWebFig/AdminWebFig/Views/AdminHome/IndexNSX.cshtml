@model IEnumerable<AdminWebFig.Models.NSX>
@{
    ViewBag.Title = "IndexNSX";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



@Styles.Render("~/Content/css")
@Styles.Render("~/Content/brand_admin.css")

@*@Styles.Render("~/Content/brand_admin")*@

@*<p>@Html.ActionLink("Thêm NSX mới", "CreateNSX")</p>*@



<div class="brand">
    <div class="container">
@*        <div class="brand-header">Quản lí nhà sản xuất</div>*@
        <div class="brand-heading">
            <div class="container">
                <table class="table">
                    <thead class="table-info">
                        <tr>
                            
                            <th class="brand-item-heading">Tên</th>
                            <th class="brand-item-heading">Hình</th>
                            <th class="brand-item-heading">Mô tả</th>
                            <th class="brand-item-heading">Chỉnh sửa</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)

                        {
                            <tr>
                            
                                <td>@item.tenNSX</td>
                                <td>
                                    <img width="120px" src="@item.hinhNSX" alt="">
                                </td>
                                <td>@item.motaNSX</td>
                                <td>
                                    @*<a href="@Url.Action("DeleteNSX","AdminHome",new { id = item.idNSX})" class="btn brand-button-item brand-delete">Xoá</a>*@
                                    <a href="@Url.Action("EditNSX","AdminHome",new { id  = item.idNSX})" class="btn brand-button-item brand-edit"> Chỉnh sửa</a>
                                </td>
                            </tr>
                        }



                    </tbody>
                </table>
                <a href="@Url.Action("CreateNSX","AdminHome")" class="btn brand-button-item brand-create">Tạo mới</a>
            </div>
        </div>
    </div>
</div>
    </div>

<script src="./detail.js"></script>
</body>

</html>
<style>
    .home-product-item__img {
        padding-top: 100%;
        background-position: center;
        background-size: contain;
        background-repeat: no-repeat;
        /* Ảnh zoom chậm lại */
        transition: 0.4s;
        will-change: transition;
        margin-top: 40px
    }

    .btn-view-all {
        width: 156px;
        height: 38px;
        font-weight: 500;
        font-size: 1.6rem;
        line-height: 32px;
        color: #7d7d7d;
        border: 2px solid #7d7d7d;
        background-color: var(--white-color);
        transition: 0.2s;
    }

    .footer {
        background-color: #171717;
    }

    .check-in {
        color: #fff;
        font-weight: 600;
        font-size: 16px;
        text-decoration: none;
    }
    .btn {
        margin-bottom: 12px;
    }

    .brand-create {
        float: right;
        margin-right: 42px;
    }
    .brand {
        margin-top: 120px;
    }

    .nav > li > a {
        padding: 0;
    }
</style>


